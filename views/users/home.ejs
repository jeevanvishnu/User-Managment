<%- include('../layout/header.ejs') %>
<h1>Welcome to <%= user.name %> </h1>

<table style="width: 50%; margin:auto; margin-top: 30px;">
    <tr>
        <td><img style=" width: 100px; height: 100px; border-radius: 50%;" src="userImage/<%= user.image  %>" alt="<%= user.image  %>"></td>
    </tr>
    <tr>
        <td>Email</td>
        <td><%= user.email %></td>
    </tr>
    <tr>
        <td>Mobile</td>
        <td><%= user.mobile %></td>
    </tr>
    <tr>
        <td>Verfied</td>
        <td>
            <% if(user.is_verified == 1){
                %>
                <p>true</p>
                <%
            }
                 %>
        </td>
    </tr>
</table>

<a class="btn btn-primary" style="margin-left: 20px; text-align: center; margin-left:384px ; margin-top: 42px;"  href="/edit?id=<%= user._id%>">Edit</a>
<a class="btn btn-danger" style="margin-left: 20px; text-align: center; margin-left:384px ; margin-top: 10px;"  href="/logout">Logout</a>